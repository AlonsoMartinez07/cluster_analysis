# Data

| Path | Description |
|------|-------------|
| `processed/maxwellian_scores_combined.csv` | Per-simulation Maxwellianity (v7, sens=500) |
| `../config.yaml` | Points to raw RGB dataset, `.nc` roots, AE weights |

**Cluster assignment CSVs** are copied under `analysis/outputs/<method>/` rather than duplicated here, to avoid stale copies.

Raw images: `D:/GENE_simulation_AI/DCEC/all_rgb`  
Raw physics: `simulation_normals/electron`, `simulation_anomalies/electron/...`
